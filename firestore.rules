rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default open access for authenticated users, read/write for local business app
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
